import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    button: {
        marginVertical: 5,
        borderRadius: 5
    },
    tip: {
        fontStyle: 'italic',
        color: 'grey',
        fontSize: 14
    }
});

export default styles;
